openai:
  api-key: ${OPENAIKEY}

tmdb:
  api:
    key: ${TMDBKEY}
    url: https://api.themoviedb.org/3

spring:
  datasource:
    url: jdbc:postgresql://ep-icy-salad-amysu2ej.c-5.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_0uvlTx3Lymgf
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    out-of-order: true
    repair-on-migrate: true
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}